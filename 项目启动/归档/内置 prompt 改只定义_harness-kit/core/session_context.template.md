# Session Context — 项目上下文配置

> **使用方法**：复制本文件到 `sessions/<session_name>/session_context.md`，填写所有标注为 `[必填]` 的字段。所有角色 Prompt 将从此文件读取项目特定信息。

---

## 1. 项目信息 [必填]

- **项目名称**：
- **一句话描述**：
- **项目类型**：[Web App | CLI Tool | Library | Mobile App | Data Pipeline | API Service | Desktop App | Other]
- **复杂度等级**：[S | M | L]（参照 bootstrap.md 评估）

---

## 2. 技术上下文 [必填]

- **主语言**：
- **框架/运行时**：
- **数据库**：[无 | SQLite | PostgreSQL | MySQL | MongoDB | Redis | Other]
- **关键外部依赖**：
- **现有代码库**：[全新项目 | 改造现有代码]
- **目标部署环境**：[本地 | 云服务 | Docker | Serverless | Other]

---

## 3. 关键约束 [必填]

- **安全性要求**：[低 | 中 | 高] — 简要说明：
- **性能要求**：[低 | 中 | 高] — 简要说明：
- **用户体验要求**：[低 | 中 | 高] — 简要说明：
- **可维护性要求**：[低 | 中 | 高] — 简要说明：
- **不可妥协的底线**：
  1. （例：密码必须加盐哈希存储）
  2. （例：所有 API 必须有认证）

---

## 4. 质量偏好（影响 Reviewer 评分权重）

| 维度 | 权重 | 说明 |
|------|------|------|
| 功能正确性 | 25% | [可调整] |
| 安全性 | 25% | [可调整] |
| 代码质量 | 15% | [可调整] |
| 测试覆盖 | 15% | [可调整] |
| 架构一致性 | 10% | [可调整] |
| 可维护性 | 10% | [可调整] |

> 权重总和必须为 100%。根据项目特性调整：安全敏感项目提高安全性权重，内部工具可降低 UX 权重。

---

## 5. 领域特定代码规范 [按需填写]

```
（在这里写该项目特定的代码规范。例如：
- Python：类型注解，函数有文档字符串
- 错误处理：只处理边界输入和外部依赖失败
- SQL 永远使用参数化查询
- JWT secret 从环境变量读取
）
```

---

## 6. 领域特定审查重点 [按需填写]

```
（在这里列出 Reviewer 应该重点检查的领域特定高风险项。例如：
- passlib 的 verify 方法是否正确使用
- JWT decode 时是否验证了签名和过期时间
- 密码字段是否在 API 响应中被排除
）
```

---

## 7. 角色专业背景补充 [按需填写]

- **Proposer 应具备的专业知识**：
  （例：Web 认证标准 OAuth 2.0/JWT/Session、OWASP Top 10）
- **Reviewer 应重点关注的风险域**：
  （例：认证绕过、会话劫持、暴力破解、SQL 注入）

---

## 8. 文件路径约定 [必填]

- **PRD 路径**：`product/prd/`
- **源码路径**：`product/src/`
- **Session 记录路径**：`sessions/`
- **进度文件**：`sessions/conductor/progress.md`
- **架构方案存放**：`sessions/conductor/tasks/task_001_architect/output.md`

---

## 9. 辩题概述（仅 M/L 复杂度） [按需填写]

- **核心辩题**：（一句话概括需要深度辩论的核心问题）
- **辩论偏好**：
  - 重点辩论层：[问题定义 | 理想态 | 差距分析 | 策略 | 全部]
  - 最关心的维度：[安全 | 性能 | 体验 | 商业可行性]
