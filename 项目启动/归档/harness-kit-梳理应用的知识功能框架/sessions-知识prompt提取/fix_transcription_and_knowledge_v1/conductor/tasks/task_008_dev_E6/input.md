# Task 008 — Dev E6（F-11 回归）

任务缩减：task_001 已验证 F-11 三命令（`synthesize_viewpoints` / `generate_extensions` / 共现关系计算）均真实写库，无 stub。

本 task 仅需：
1. 不改源码
2. 确认 cargo test --lib 全绿
3. 列出推荐的运行时回归矩阵（W/V/I/K/E）供后续 QA
