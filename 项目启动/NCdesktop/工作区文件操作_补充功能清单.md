# 工作区文件操作 — 待补充功能清单

> 关联 PRD：`项目启动/归档/harness-kit-.../sessions/workspace_drag_v1/prd/workspace_drag_prd_v2.1.md`  
> 关联 Architect v2 输出：`.../tasks/task_007_architect_v2/output.md`  
> 生成时间：2026-05-12

---

## 当前实现度速览

| 段 | 完成度 |
|----|--------|
| P0 MVP | ≈ 90%（功能齐，留 2 个尾巴） |
| P1 迭代一 | 0%（未启动） |

P0 主路径（F-01 修 bug / F-02 左栏拖拽 / F-04 右键菜单 / F-05 二级子菜单 / F-06 Rust 原子 move）**已验收 + 打包**。

---

## 需要补充的功能（按优先级）

### 🔴 P0 尾巴（已部分完成，需收口）

#### 1. Cmd+A 焦点分流（F-03）
- **现状**：`AssetListView.tsx` 已埋 `leftPaneFocused` 状态和 `onMouseEnter/Leave` 触发器，但 keydown handler（约 L298-L310）的全选逻辑没有按焦点分流，仍统一用 `displayAssets`。
- **需要做**：handler 内根据 `leftPaneFocused` 在 `rawAssets`（左栏） / `processedAssets`（右栏）之间切换全选源。
- **对应 Task**：`task_008_verify_cmda_focus`

#### 2. 移动成功 / 失败 Toast（F-04 / F-05）
- **现状**：`AssetContextMenu.handleMoveToFolder` 成功后只调 `onMoved()`，没有触达 `useUIStore.addNotification`，用户感知不到"成功"。失败也只打印 console.error。
- **需要做**：成功 → `type:'success'` Toast；失败 → `type:'error'` Toast 并保留菜单状态。
- **对应 Task**：`task_009_move_toast`

---

### 🟡 P1 迭代一（完全未启动）

#### 3. 复制文本内容 — Rust 命令（F-07a）
- **现状**：`src-tauri/src/commands/asset.rs` 无 `read_asset_text_content`；`src/lib/tauri-commands.ts` 无 `readAssetTextContent` 包装。
- **需要做**：Rust 命令按优先级链读取（Markdown 文件全文 → `analysis.ocrText` → `analysis.summary` → 空串），返回 `Vec<String>`，前端 TS 包装导出。
- **对应 Task**：`task_010_rust_read_text`

#### 4. 复制文本内容 — UI 入口（F-07b）
- **现状**：`BatchToolbar` 仅有移动/复制项目/删除三个按钮，**无"复制文本"按钮**；右键菜单（`AssetContextMenu`）右栏无"复制文本内容"菜单项（PRD §F-04 右栏菜单表明确要求）。
- **需要做**：
  - `BatchToolbar` 右栏新增"复制文本"按钮（带 `lucide-react` icon）。
  - `AssetContextMenu` 在 `pane === 'right'` 时插入"复制文本内容"菜单项（位于"在 Finder 中显示"上方）。
  - 点击后调 `readAssetTextContent` → `\n---\n` 拼接 → `navigator.clipboard.writeText` → Toast 反馈。
  - 空内容用 `[尚无文本内容]` 占位。
- **对应 Task**：`task_011_copy_text_ui`（依赖 task_010）

#### 5. 拖拽到 app 内文件夹 Spike（F-08）
- **现状**：未启动，PRD 明确这是 Spike，**不是正式实现**。
- **需要做**：临时注入 DnD 事件监听器，验证 PRD §F-08 三个 Spike 通过条件，产出 `spike_report.md`，**不修改 startDrag 主路径**。
  - 全通过 → 立项正式实现 task（编号待定）。
  - 任一失败 → 维持现有右键菜单，本 Spike 关闭。
- **对应 Task**：`task_012_drag_spike`

---

## Task 依赖与并行度

```
P0 尾巴（独立，可并行）
  ├── task_008_verify_cmda_focus
  └── task_009_move_toast

P1 主线（串行）
  └── task_010_rust_read_text ──► task_011_copy_text_ui

P1 Spike（独立）
  └── task_012_drag_spike
```

**可并行**：`{task_008, task_009, task_010, task_012}` — 4 个 Task 互不依赖。

---

## 建议下一步动作

| 顺序 | 动作 |
|------|------|
| 1 | 先做 P0 尾巴（task_008 + task_009），约 1 小时内可完成 |
| 2 | task_012 Spike 与 P0 尾巴并行（独立探针，无代码冲突） |
| 3 | Spike 结果指导 P1 主线：通过则做完 task_010+011 后立项 drag-to-folder 正式实现；失败则只做 task_010+011 |

---

## 文件索引

- Architect v2 完整输出：`项目启动/归档/.../sessions/workspace_drag_v1/conductor/tasks/task_007_architect_v2/output.md`
- 各 Task input.md：`项目启动/归档/.../sessions/workspace_drag_v1/conductor/tasks/task_008..012_*/input.md`
- 原 PRD：`项目启动/归档/.../sessions/workspace_drag_v1/prd/workspace_drag_prd_v2.1.md`
- 历史 progress：`项目启动/归档/.../sessions/workspace_drag_v1/conductor/progress.md`（需在 Task 启动时追加 P1 段状态）
